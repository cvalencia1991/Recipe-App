<h1>recipe show</h1>

    <div>
        <%= @recipe.name%>
    </div>
    <div>
        <div>
            prearation time: <%= @recipe.preparation_time%> hour
        </div>
        <div>
            Public: <%= check_box_tag checked: @recipe.public %>
        </div>
    </div>
    Cooking time: <%= @recipe.cooking_time%> hour
    <div>
    Steeps go here...
    </div>
    <%= link_to "foods", user_recipe_foods_path(params[:user_id],params[:id])%>
    <%= link_to "add ingredeints", new_user_recipe_recipe_food_path(params[:user_id],params[:id])%>
    <% @recipefoods.each do |foods| %>
        <div>
            <%= foods.food.name %>
        </div>
            <%= link_to "Remove",user_recipe_food_path(params[:user_id], params[:id], foods.id),data: {turbo_method: :delete}  %>
            <%= link_to "Modify",edit_user_recipe_path(params[:user_id], params[:id], foods.id), method: :edit  %>
    <% end %>






